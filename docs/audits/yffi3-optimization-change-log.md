# YFFI3 production optimization change log

All dates use America/New_York unless noted.

| Date | Change | Baseline | Expected measurable benefit | Pre-deploy tests | Live verification | Rollback |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-05 | Add `utm_id` and `utm_term` to sanitized first-touch event context | Production interaction events expose source, medium, campaign, and content but omit UTM campaign ID and paid-search term | More complete campaign-level attribution with no visual or interaction change | Build, validation suite, interaction-event payload test, no GCLID/PII in `dataLayer` | Pending deployment; verify GTM preview and GA4 DebugView event parameters | Revert the two map entries and payload parameters |
| 2026-08-05 | Preserve committed `assets/site.js` during generator/build and make localization idempotent | `generate-live-base.mjs` contains an older fallback runtime, while the localization pass can duplicate already-applied Save-Data/form helpers | Prevent performance/accessibility regressions and invalid duplicate runtime declarations; generated runtime must remain byte-identical to committed runtime | Run generation twice, compare runtime before/after, build, and run full validators | Pending deployment; compare live asset and production funnel | Revert generator/idempotency guards, then restore previous runtime if needed |
| 2026-08-05 | Define safe attribution/lifecycle contract and local CSV joiner | Existing automation maps only `utm_source` and treats a redirect/form submit as a lead | Truthful visit-to-lead-to-bind-to-revenue reporting with dedupe and privacy gates | Local reconciliation positive/negative tests; document vendor/OAuth blockers | No production behavior change; activation remains gated | Keep vendor handoff disabled; retain current quote URL |
| 2026-08-05 | Add verified `RV insurance` service to Google Business Profile | GBP listed the website's other verified insurance offerings but omitted RV insurance | Improve service/entity completeness for relevant local discovery without adding a category, location, or unverified claim | Compared signed-in GBP services against the published website and `InsuranceAgency` catalog | `PENDING GOOGLE REVIEW`; Google indicated review may take up to one day. No publication claim until rechecked | Remove only the custom `RV insurance` service in GBP if rejected or found inaccurate |
| 2026-08-05 | Align authoritative site facts with the 13 insurance service concepts already published in structured data | `content/site-facts.json` and its schema listed only six services while the generated public `InsuranceAgency` offer catalog listed 13 | Prevent generated GEO/AI-readable surfaces from omitting verified offerings; no visual change | Validate JSON/schema, regenerate twice, compare committed runtime hash, build and repository checks | Pending deployment; inspect live JSON-LD, API/Markdown representations, and validator output | Revert the `insuranceTypes` arrays in the fact file and schema |
| 2026-08-05 | Refine English and Spanish home-page H1s from statewide wording to Miami intent | Titles, descriptions, body copy, NAP, and service pages target Miami, but each home-page H1 led with Florida | Better alignment with current qualitative Miami agency search intent and the verified physical office; same typography and component structure | Regenerate, build, validate one H1 per page, and compare desktop/mobile English/Spanish screenshots for wrapping or layout regression | Pending deployment; inspect rendered H1s and production layout at matching viewports | Restore `Florida Insurance Made Simple for Your Family` and `Seguros de Florida más sencillos para su familia` in source content and generator |
| 2026-08-05 | Establish current production RUM baseline and preserve measured-good design behavior | Cloudflare last-24-hour baseline: 36 visits, 46 page views, 951 ms page load; LCP P75 889 ms with 88% good, INP 100% good, CLS 100% good | Prevent speculative design degradation and make later performance work accountable to field data | Record Cloudflare RUM and existing lab regression metrics; build/validate runtime safeguard | No deploy required for the baseline; recheck the same RUM view after any material performance release | Revert only the performance change that regresses RUM/lab/visual results; keep the baseline record |
| 2026-08-05 | Publish GTM version 7 with `sms_click` in the YFFI marketing-intent trigger | Site runtime emitted `sms_click`, but live GTM version 6 ignored it; SMS intent could not reach GA4 | Complete measured call/SMS/quote channel attribution without a site-design or behavior change | Saved only the trigger regex change; Tag Assistant draft preview showed event 6 `sms_click` and the GA4 Event tag fired once; GA4 Realtime received the event | `CONNECTED`: version 7 is live; a clean non-preview production SMS-link test increased GA4 Realtime `sms_click` key events from 1 to 2 | Publish GTM version 6 as the prior live version |
| 2026-08-05 | Localize the Spanish call-to-text conjunction from `or` to `o` | English `or` appeared between the otherwise localized call and SMS links on the Spanish home and quote pages | Remove a visible bilingual trust defect with no layout, destination, or tracking change | Regenerate twice, verify the exact Spanish phrase on both pages, build, validate links/events, and compare desktop rendering | Pending deployment; inspect `/es/` and `/es/solicitar-cotizacion/` on the custom domain | Revert the single localization pair and regenerate |
| 2026-08-05 | Synchronize browser QA expectations with the approved campaign fields and Miami H1 | The first main-branch workflow correctly rejected stale assertions for the old field set and Florida H1; Firefox also exposed a two-pixel fractional-rounding false positive | Restore a meaningful required production gate without weakening privacy or visible-overlap detection | Keep the exact allow-list assertion, explicitly test sanitized `utm_id`/`utm_term`, retain the GCLID exclusion, update the expected H1, and allow only a two-pixel cross-engine rounding tolerance | Pending second workflow | Revert this test-only commit; production bytes remain on the prior successful deployment until CI passes |
| 2026-08-05 | Isolate first-party Lighthouse budgets from the external GTM loader | All 274 applicable cross-browser tests passed, but all 12 Lighthouse runs scored Best Practices 0.74 because the approved CSP blocked a Google Ads view-through script requested through GTM | Preserve strict CSP and make the local performance gate measure first-party regressions; live analytics remains a separate authenticated verification | Block only `https://www.googletagmanager.com/gtm.js*` in Lighthouse collection; retain the exact GTM source test, Tag Assistant evidence, GA4 Realtime evidence, and production RUM baseline | Pending branch workflow; no production security/header change | Remove the single Lighthouse `blockedUrlPatterns` entry |
